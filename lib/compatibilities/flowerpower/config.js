module.exports = {
  updateIntervalAirTemperature: 60, // Interval between updating air temperature data (in min) (default 1h)
  updateIntervalSoilTemperature: 60, // Interval between updating soil temperature data (in min) (default 1h)
  updateIntervalLight: 60, // Interval between updating light data (in min) (default 1h)
  updateIntervalMoisture: 120, // Interval between updating soil moisture value (in min) (default 2h)
  updateIntervalSoilEC: 120, // Interval between updating electrical conductivity value (in min) (default 2h)
  updateIntervalBatteryLevel: 1440, // Interval between updating battery level data (in min) (default 1 day)
};