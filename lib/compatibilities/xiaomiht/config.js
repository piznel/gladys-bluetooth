module.exports = {
    updateIntervalTemperature: 30, // Interval between updating temperature data (in min) (default 30mins)
    updateIntervalMoisture: 30, // Interval between updating moisture value (in min) (default 30mins)
    updateIntervalBatteryLevel: 1440, // Interval between updating battery level data (in min) (default 1 day)
};